version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11

  build:
    commands:
      - pip install -r requirements.txt -t .
      - zip -r lambda.zip .

  post_build:
    commands:
      - aws lambda update-function-code --function-name apigateway-processor --zip-file fileb://lambda.zip